AGENT STARTER PACK - QUICK INSTALL (v1.8.0)
============================================
Human read-first. Agents: HANDOFF_NEXT_AGENT.md + docs\WORK_QUEUE.md (status).

NEW PC OR FLASH DRIVE (WQ-011)
--------------------------------
1. cd to the pack folder on the stick (drive letter varies).
2. exFAT + git only:  git config --global --add safe.directory E:/AgentStarterPack
3. .\Check-Requirements.cmd          (-Fix if Python deps missing)
4. .\Install-AgentStarterPack.cmd
   or:  .\install.ps1 -Scope User -NoPause -Prune
5. Restart Cursor fully.
6. Verify (expect 0 fail):
     .\pack\scripts\doctor.ps1 -ProjectRoot (Get-Location).Path
     .\pack\scripts\verify-audit-behavior.ps1

THIS PC (already have the folder)
---------------------------------
1. Install-AgentStarterPack.cmd
2. Restart Cursor
3. doctor.ps1 + verify-audit-system.ps1  (see INSTALL.md)

INSTALLS TO (per machine, not portable)
  %USERPROFILE%\.cursor\AgentStarterPack
  %USERPROFILE%\.cursor\rules
  %USERPROFILE%\.cursor\skills
  %USERPROFILE%\.cursor\mcp.json

VERSIONS (canonical)
  Pack:   root VERSION  (currently 1.8.0)
  Audit:  pack\audit\manifest.json  (currently 2.22.43)
  Queue:  docs\WORK_QUEUE.md  (Next: WQ-011)

MORE
  INSTALL.md              full install + upgrade
  HANDOFF_NEXT_AGENT.md  agent handoff (architecture, commands)
  pack\docs\START_HERE.md maintainer onboarding

CURSOR CHAT
  "Run agent_hygiene_full_check"
  After killing a hung terminal: cleanup orphan agent processes dry run first
