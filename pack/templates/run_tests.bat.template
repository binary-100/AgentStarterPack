@echo off
REM Thin wrapper. The implementation is scripts\run_tests.ps1, shared with run_tests.sh, so both
REM platforms run the same suite instead of two runners that drift apart.
cd /d "%~dp0"
powershell -NoProfile -ExecutionPolicy Bypass -File "scripts\run_tests.ps1" %*
exit /b %ERRORLEVEL%
